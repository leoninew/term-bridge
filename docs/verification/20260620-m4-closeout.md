# M4 待办事项收口验证
最后修改时间: 2026-06-20 17:34:36

Review status: Accepted

## Requirement alignment

依据：`docs/requirement/20260620-m4-closeout.md`

- [x] `docs/design.md` 记录当前本地入口为 `termbridge [options] exec -- <command...>`。
- [x] `docs/design.md` 的设计原则与 CLI-first、exec 子命令和 local Web/workbench 边界一致。
- [x] `docs/plan/20260617-roadmap-refresh.md` 中 M1/M2/M2.5/M3/M4 状态与当前实现对齐。
- [x] roadmap 中 CLI 示例使用 `termbridge exec -- <command...>` 或 `termbridge --cwd <dir> exec -- <command...>`。
- [x] roadmap 中列出 M4 closeout checklist，并说明可自动化项和人工验证项边界。
- [x] roadmap 说明不实现显式 `recent` 命令或新 UX；session metadata/listing/history 提供基础 recent 可见性。
- [x] 可自动化的 M4 product-surface 检查已补充到单元测试。
- [x] README 展示当前使用方式。
- [x] M5 hardening 风险保留为 M5 项，没有标为 M4 已完成。

## Spec alignment

不适用。当前流程为轻量模式 / light，无单独 spec 文档。

## Plan alignment

不适用。当前流程为轻量模式 / light，无单独 plan 文档；实现按 accepted requirement 执行。

## Actual diff summary

本次变更：

- 更新 `docs/design.md`：同步当前 CLI 入口、设计原则、local Web/workbench 边界和后续决策项。
- 更新 `docs/plan/20260617-roadmap-refresh.md`：同步 M1-M4 状态、CLI 示例、M4 closeout checklist、M5 风险承接。
- 更新 `README.md`：展示当前 CLI、session/workspace 和 Web workbench 使用方式。
- 新增 `docs/requirement/20260620-m4-closeout.md`：记录 M4 收口需求和用户决策。
- 增强 `internal/cli/cli_test.go`：覆盖 help 中的当前命令、子命令和示例。
- 增强 `internal/app/app_test.go`：覆盖 session list 中 command、cwd、log path 等基础产品面信息。

## Expected vs actual changed files

| File | Expected | Actual | Notes |
|---|---:|---:|---|
| `docs/design.md` | Yes | Yes | 当前入口和设计原则已更新 |
| `docs/plan/20260617-roadmap-refresh.md` | Yes | Yes | M1-M4 状态和 M4/M5 边界已更新 |
| `README.md` | Yes | Yes | 用户入口示例已更新 |
| `docs/requirement/20260620-m4-closeout.md` | Yes | Yes | SpecFlow requirement 已创建并接受 |
| `internal/cli/cli_test.go` | Yes | Yes | 补充可自动化 help 检查 |
| `internal/app/app_test.go` | Yes | Yes | 补充 session list product surface 检查 |

## Acceptance criteria checklist

- [x] 设计文档当前入口为 `termbridge [options] exec -- <command...>`。
- [x] 设计文档保留 CLI-first 和 runtime ownership 边界。
- [x] roadmap 标记 M1 已完成、M2 核心完成、M2.5 已实现、M3 主体完成、M4 收口中。
- [x] roadmap 说明 M5 接收 runtime hardening 风险。
- [x] roadmap 不要求实现显式 `recent` 命令或新 UX。
- [x] roadmap 将可自动化 verification 项归入单元测试，将交互/runtime 场景保留为人工验证项。
- [x] README 展示当前使用命令。
- [x] 单元测试覆盖 CLI help 和 session list product surface。

## Test results

已运行：

```text
go test ./internal/cli ./internal/app
```

结果：

```text
ok  	termbridge-go/internal/cli	(cached)
ok  	termbridge-go/internal/app	(cached)
```

一致性检查：

```text
Grep docs/design.md for: termbridge [options] --, termbridge -- claude, termbridge -- <, 旧入口, 废弃
Grep docs/plan/20260617-roadmap-refresh.md for: termbridge [options] --, termbridge -- claude, termbridge -- <, 旧入口, 废弃
Grep README.md for: termbridge -- claude, termbridge [options] --, 旧入口, 废弃
```

结果：无匹配。

## Missed or expanded scope

- 未修改产品功能代码。
- 未实现显式 `recent` 命令或 logs 命令。
- 未处理 M5 runtime hardening 问题。
- 未进入 Gateway / M6 实现。

## Risks

- M4 closeout 文档已完成，但 M5 中仍需验证真实 TUI、Ctrl+C、大输出、backpressure、resize、进程树清理和长时间运行。
- 当前测试为单元测试层面，未覆盖真实 PTY 交互和 Web terminal 交互。

## Incomplete items

以下项目进入 M5：

1. Claude Code / Codex 真实 TUI 交互。
2. Ctrl+C 在 Claude Code / Codex / shell 中的真实 interrupt 行为。
3. 大量输出、slow client backpressure、高频 resize。
4. process tree cleanup 和长时间运行稳定性。
5. local Web/workbench 输入、输出、resize、attach/detach、history replay 人工验证。

## Conclusion

M4 文档和可自动化 product-surface 检查已收口。M4 可关闭，后续进入 M5 Runtime Hardening。