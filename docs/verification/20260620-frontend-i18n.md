# 前端引入 i18n 验证
最后修改时间: 2026-06-20 13:58:52

Review status: Accepted

## Requirement alignment

基于 `docs/requirement/20260620-frontend-i18n.md`，Review status: Accepted。

| Requirement | Result | Notes |
|---|---|---|
| 引入 `vue-i18n` 依赖，并在 Vue app 中注册 i18n 实例 | Pass | `web/package.json` / `web/yarn.lock` 新增 `vue-i18n`；`web/src/main.ts` 使用 `.use(i18n)` 注册。 |
| 存在 `zh-CN` 与 `en-US` 两套基础 message | Pass | `web/src/i18n.ts` 定义两套 message。 |
| 默认 locale 为 `zh-CN` | Pass | `web/src/i18n.ts` 的 fallback/default 为 `zh-CN`；如 localStorage 无有效值则使用 `zh-CN`。 |
| 左下角设置区域提供语言切换入口，并使用合适的 reka-ui 组件 | Pass | `WorkspaceSessionSidebar.vue` footer 使用 reka-ui `SelectRoot` / `SelectTrigger` / `SelectContent` 等组件切换语言。 |
| `App.vue` 中主要可见静态 UI 文案迁移到 i18n | Pass | 主工作台、dialog、footer、toast title、校验提示、aria-label 已迁移为 `t(...)`。 |
| `WorkspaceSessionSidebar.vue` 中主要可见静态 UI 文案和 aria-label 迁移到 i18n | Pass | sidebar 搜索、空状态、节点操作 aria-label、设置文案已迁移为 `t(...)`。 |
| API client 中面向 UI 的错误 prefix/title 有清晰策略 | Pass with note | Vue 组件中的 toast title 已迁移；API client 内的英文 error prefix 保留为技术错误前缀，因为其不在 Vue i18n 注入上下文内。 |
| 不翻译 workspace/session 用户数据和后端动态错误 message | Pass | session/workspace name、command、cwd、path、后端 error message 仍原样展示。 |
| `yarn --cwd web typecheck` 通过 | Pass | 实际命令使用绝对路径运行并通过。 |
| `yarn --cwd web lint` 通过 | Pass | 实际命令使用绝对路径运行并通过。 |

## Spec alignment

不适用。轻量模式 / light 未创建独立 spec，按 requirement / 需求核对。

## Plan alignment

不适用。轻量模式 / light 未创建独立 plan，按 requirement / 需求核对。

## Actual diff summary

本次 i18n 相关实际改动：

- `docs/requirement/20260620-frontend-i18n.md`
  - 新增 i18n 轻量需求文档并标记为 `Accepted`。
- `web/package.json`
  - 新增 `vue-i18n` dependency。
- `web/yarn.lock`
  - 锁定 `vue-i18n` 及其依赖。
- `web/src/i18n.ts`
  - 新增 i18n 初始化、`zh-CN` / `en-US` messages、locale labels、默认 locale、localStorage 保存和 `setLocale()`。
- `web/src/main.ts`
  - 注册 i18n plugin。
- `web/src/App.vue`
  - 使用 `useI18n()`。
  - 迁移主工作台、dialog、footer、toast title、校验提示和 aria-label。
- `web/src/components/workspace/WorkspaceSessionSidebar.vue`
  - 使用 `useI18n()`。
  - 迁移 sidebar 文案和 aria-label。
  - 在左下角设置区域增加 reka-ui Select 语言切换入口。

## Expected vs actual changed files

| Expected | Actual | Notes |
|---|---|---|
| `docs/requirement/20260620-frontend-i18n.md` | Changed | 符合 SpecFlow light requirement 记录。 |
| `web/package.json` | Changed | 新增 `vue-i18n`。 |
| `web/yarn.lock` | Changed | 新增依赖锁定。 |
| `web/src/i18n.ts` | Added | i18n 初始化和 messages。 |
| `web/src/main.ts` | Changed | 注册 i18n。 |
| `web/src/App.vue` | Changed | 文案迁移。 |
| `web/src/components/workspace/WorkspaceSessionSidebar.vue` | Changed | 文案迁移和语言切换入口。 |
| 后端文件 | Not changed | 符合不改后端的要求。 |

## Acceptance criteria checklist

- [x] 引入 `vue-i18n` 依赖，并在 Vue app 中注册 i18n 实例。
- [x] 存在 `zh-CN` 与 `en-US` 两套基础 message。
- [x] 默认 locale 为 `zh-CN`。
- [x] 左下角设置区域提供语言切换入口，并使用合适的 reka-ui 组件。
- [x] `App.vue` 中主要可见静态 UI 文案迁移到 i18n。
- [x] `WorkspaceSessionSidebar.vue` 中主要可见静态 UI 文案和 aria-label 迁移到 i18n。
- [x] 前端 API client 中面向 UI 的错误 prefix/title 有清晰策略：UI toast title 已迁移，API client 技术前缀保留。
- [x] 不翻译 workspace/session 用户数据和后端动态错误 message。
- [x] `yarn --cwd web typecheck` 通过。
- [x] `yarn --cwd web lint` 通过。

## Test results

### Web typecheck

Command:

```sh
yarn --cwd /Volumes/media/SourceCodes/mywork/TermBridge-go/web typecheck
```

Result: Pass。

Output summary:

```text
$ vue-tsc --noEmit
Done in 6.09s.
```

### Web lint

Command:

```sh
yarn --cwd /Volumes/media/SourceCodes/mywork/TermBridge-go/web lint
```

Result: Pass。

Output summary:

```text
$ eslint .
Done in 5.30s.
```

### Hardcoded UI text scan

Command scope:

- Grep checked common migrated UI strings across `web/src/**/*.{vue,ts}`.

Result:

- Expected matches remain in `web/src/i18n.ts` message definitions.
- Expected technical/API matches remain in `web/src/features/*/api.ts` as error prefixes.
- Vue component matches are either code identifiers/events or `t(...)` calls.

## Missed or expanded scope

Expanded scope:

- Locale selection is persisted in `localStorage` as `termbridge.locale` even though backend persistence was explicitly out of scope. This remains frontend-local and aligns with the requirement note that local storage is preferred if persistence is needed.

Missed scope:

- 未做浏览器手工切换语言验证。
- 未迁移 API client 内部英文 error prefix 到 i18n；已按技术前缀保留并记录原因。

## Risks

1. 当前工作树中仍包含之前 workspace tree actions / workspace 排序相关改动，i18n diff 与相邻 UI 改动共存；提交前需要确认是否一起提交或拆分。
2. API client 英文 error prefix 会出现在组合错误 message 中；UI toast title 已本地化，但完整技术错误仍可能包含英文前缀。
3. `localStorage` 访问发生在 `web/src/i18n.ts` 模块初始化阶段，当前 Vite 浏览器前端可用；若未来引入 SSR，需要调整。
4. 语言切换入口未做浏览器交互手工验收。

## Incomplete items

- 未执行浏览器手工验收。
- 未执行 git add / commit / push。

## Conclusion

验证通过。i18n 基础设施、默认 `zh-CN`、`zh-CN`/`en-US` message、左下角 reka-ui 语言切换入口，以及主要 UI 文案迁移均与 requirement 对齐。typecheck 与 lint 均通过。
