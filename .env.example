# TermBridge-go 配置
# 复制为项目根目录下的 .env 后生效。
# 配置优先级：.termbridge.default.yaml < .termbridge.yaml < .env < OS env。
# .env 不会覆盖 Docker/systemd/Kubernetes/CI/shell 已注入的同名环境变量。
# 命名规则：TERMBRIDGE_ + 配置 key 大写，key 中的 . 转为 __。
# 示例：gate.listen_url -> TERMBRIDGE_GATE__LISTEN_URL


# JWT 认证密钥（必填）
# 生成方式：openssl rand -base64 32
# TERMBRIDGE_JWT__SECRET_KEY=your-base64-encoded-32-byte-random-string


# 日志
# TERMBRIDGE_LOG__LEVEL=info
# TERMBRIDGE_LOG__FORMAT=text
# TERMBRIDGE_LOG__DIR=logs
# TERMBRIDGE_LOG__HTTP__REQUEST_BODY_LIMIT=4096
# TERMBRIDGE_LOG__HTTP__RESPONSE_BODY_LIMIT=4096

# 历史记录
# TERMBRIDGE_HISTORY__MAX_LINES=10000
# TERMBRIDGE_HISTORY__MAX_BYTES=5242880
# TERMBRIDGE_HISTORY__MAX_LINE_BYTES=65536

# Runtime 状态目录
# TERMBRIDGE_RUNTIME__STATE_DIR=.termbridge

# Gate
# TERMBRIDGE_GATE__LISTEN_URL=http://127.0.0.1:9010
# 多个 origin 使用逗号分隔。
# TERMBRIDGE_GATE__BROWSER__ALLOWED_ORIGINS=http://localhost:9011
# TERMBRIDGE_GATE__API__EXPOSE_ERRORS=false

# Agent
# 留空时 agent.connect_url 默认使用 gate.listen_url；如果需要覆盖，请取消注释。
# TERMBRIDGE_AGENT__CONNECT_URL=https://gate.example.com
# TERMBRIDGE_AGENT__DEVICE_ID=local-device-id
# TERMBRIDGE_AGENT__DEVICE_NAME=local-device-name