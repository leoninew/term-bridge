.DS_Store

.termbridge/
# Root runtime env files are machine-local and may contain secrets.
/.env
/.env.*
!/.env.example

# Frontend Vite mode config files are committed build inputs.
!/web/.env.development
!/web/.env.local
!/web/.env.cloud
/web/.env.production
.pomelo-pw/output/

data/
bin/
logs/