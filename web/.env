# Shared Vite defaults loaded by both dev and build.
# Do not put local dev proxy API bases here, because production builds also load this file.
# All keys must carry the TERMBRIDGE_ prefix (see vite.config.ts envPrefix).
